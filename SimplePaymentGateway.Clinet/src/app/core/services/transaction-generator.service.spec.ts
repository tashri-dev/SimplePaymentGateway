import { TransactionGeneratorService } from './transaction-generator.service';

describe('TransactionGeneratorService', () => {
  it('should create an instance', () => {
    expect(new TransactionGeneratorService()).toBeTruthy();
  });
});
